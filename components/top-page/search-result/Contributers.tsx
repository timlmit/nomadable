import styled from "styled-components";

import * as cons from "../../../constants";

interface Props {}

export const Contributers: React.FC<Props> = ({}) => {
  return <ContributersWrapper></ContributersWrapper>;
};

const ContributersWrapper = styled.div``;
