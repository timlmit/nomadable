import React from "react";
import styled from "styled-components";

import * as cons from "../../constants";

interface Props {}

export const TopPage: React.FC<Props> = ({}) => {
  return <TopPageWrapper>top page</TopPageWrapper>;
};

const TopPageWrapper = styled.div``;
